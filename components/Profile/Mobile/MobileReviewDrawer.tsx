import React from "react";

const MobileReviewDrawer = ({
  openReviewDrawer,
  setOpenReviewDrawer,
  user,
  selectedProduct,
  queuedReviews,
  setQueuedReviews,
  productReviews,
  setProductReviews,
  productsFetch,
}: any) => {
  return <div>MobileReviewDrawer</div>;
};

export default MobileReviewDrawer;
